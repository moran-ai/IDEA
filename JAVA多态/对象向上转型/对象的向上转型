对象的向上转型，就是多态
格式：父类名称 对象名 = new 子类名称();
含义：右侧创建一个子类对象，当作父类使用
注意事项：对象的向上转型一定是安全的   【从小的范围到大的范围】
类似于：double num = 100; // int -- > double 自动类型转换

对象向上转型是安全的，正确的，但是有一个弊端:
    对象一旦向上转型成为父类，就无法调用子类原有的方法